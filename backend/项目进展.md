

## 2026-07-01 发送计划页 五点交互(预览提示/已发部分回归/KH加密/公司销售联合/跳过简化)

- 预览排期: 清单已有排期时弹中文提示(确定=清空重排/取消=追加不重复不影响已有); 清单为空不提示; 加浮动中文说明。清空排期按钮移到按钮行(配置输入与动作按钮拆两行)。
- 已发部分: 每条规则右侧重新加回选择器(补发未发/全部重发/跳过, 默认补发未发); 去掉全局③默认与"走统一配置"歧义; 预览按规则各自的模式排。
- KH 加密: 地址栏不再明码。后端 _mail_kh_encode/decode(HMAC派生keystream异或+urlsafe base64) + /kh-token /kh-open; 前端 resolveKh 优先 ?e=(解密), ?id=/?kh=(明码)用后立即换成 ?e= 密文(replaceState 隐藏)。loadSuite/current-staff 用解密后的 MS_KH。往返验证通过。
- 公司名+销售联合限制: 核验 _autosend_load_contacts_for_sales 为 Owner=该销售 AND 公司名LIKE, 即"这家公司里归属该销售的联系人", 非公司全部联系人。已符合, 无需改。
- 跳过显示简化: 只显示"跳过 N 人", 明细收进 ⓘ 悬停提示。
- 验证: 内联JS node --check OK; main.py ast.parse OK; KH 密文往返 OK。
