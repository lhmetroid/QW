# 邮件 V8 有效复测与结果分析

时间：2026-06-04

## 结论

邮件 V8 最终有效复测已完成，最终有效记录为 `mail_iteration_run` v14：

- run_id：`ecf5a1aa-7c08-4779-aec7-ea1b3bfc2609`
- run_label：`v8_final_signature_dedup_deepseek_no_proxy`
- 状态：`success`
- 完成度：20/20
- 平均分：99.2
- 最低分：96.0
- 最高分：100.0
- 平均耗时：7577 ms
- LLM 成功：20/20
- fallback：0
- 真发邮件：未启用，仍为草稿与人工审核链路

## 本轮修复

- `backend/config.py` 新增邮件草稿专用 LLM 配置：
  - `MAIL_DRAFT_LLM_API_URL`
  - `MAIL_DRAFT_LLM_API_KEY`
  - `MAIL_DRAFT_LLM_MODEL`
  - `MAIL_DRAFT_LLM_TIMEOUT_SECONDS`
  - `MAIL_DRAFT_LLM_TEMPERATURE`
- `backend/main.py` 的邮件草稿 LLM 调用改为优先读 `MAIL_DRAFT_LLM_*`，未配置时回落到 `LLM2_*`。
- `backend/main.py` 邮件草稿 LLM 请求使用 `requests.Session()` 且 `trust_env=False`，避免本机/服务器系统代理把 LLM 请求导向错误代理。
- 邮件 Prompt 新增硬性规则：LLM 只输出正文段落，不输出落款、签名、发件人姓名、部门名称或签名信息。
- 后端追加统一签名前，对 LLM 段落末尾误写的销售签名做清理，降低重复落款风险。

## 结果分析

最终有效 v14 统计：

- 正文长度：最短 190 字，平均 314.2 字，最长 521 字。
- 段落数：最少 4 段，平均 4.5 段。
- 评分分布：16 封 100 分，4 封 96 分。
- 20 封全部命中 Few-Shot。
- 20 封全部使用 `deepseek-chat` 成功生成。
- 20 封全部命中本次目标业务表达。
- 历史跟进混写检查：0 封命中发票/对账/合同交付等历史跟进泄漏。
- 安全门：20 封全部 `passed`，草稿仍锁定人工审核，不真实发送。

## 质量复核修正

2026-06-04 用户基于页面截图复核后，原“已达成”的判断不成立。后续邮件迭代必须以页面可见的邮件正文结果为准，不能只看运行成功率、均分、段落数或关键词命中。

用户复核结论：

- v14 与 v7 的页面正文肉眼几乎没有区别，不能因为 v14 分数更高就判断质量问题已解决。
- “知识库不要混在历史跟进里”只是过程要求；结果要求是邮件正文必须直接、明确、稳定地运用知识库中的案例段。v14 正文仍缺少可见的知识库案例，主要还是泛泛描述经验和服务能力。
- 以案例1老客户为例，正文仍主要围绕翻译/本地化表达，没有把同传、多媒体本地化、排版印刷等非翻译新业务作为明确主推内容。
- 后续迭代验收标准必须改为“页面正文是否体现用户要求”：是否直接引用/改写知识库案例、是否明确主推新业务、是否和 v7 有肉眼可见差异。

仍需注意：

- 部分老客户唤醒/新接手联系人邮件会在正文自我介绍中提到销售姓名或部门，再由后端统一追加签名；这不是重复签名块，但视觉上仍可能显得重复。后续可继续把“正文自我介绍”和“签名”表达做得更自然。
- 本轮因本机存在多个旧隐藏 uvicorn 进程且当前会话无权限结束，先后产生了 v8-v12 的失败诊断记录。有效质量结果以 v14 为准。

## 调用环境说明

- v14 不是直接调用生产域名 `api.speedasia.net` 触发的。
- v14 使用本机临时高端口服务触发，目的是确保服务进程加载当前工作区最新代码；当时 8071 等旧本地进程仍在监听且当前会话无权限结束。
- 因此 v14 只能说明本机最新代码链路跑通，不能直接等同于生产 `api.speedasia.net` 已加载同一代码或页面结果已改善。

## 失败诊断记录

- v8/v9/v10：旧 8071 进程仍指向 DeepSeek，且系统代理不可达，报 `ProxyError`。
- v11：邮件草稿专用配置已生效，但本地 qwen 端点仍被系统代理导向 `127.0.0.1:9`，失败。
- v12：关闭系统代理后，本地 qwen 端点对 `/chat/completions` 不稳定，先超时后返回 404。该端点不适合作为 OpenAI Chat Completions 兼容出口。
- v13：使用 DeepSeek 且关闭系统代理后成功，发现部分输出有重复签名风险。
- v14：补充禁止/清理签名后成功，作为最终有效 V8 结果。
