# 开发邮件找回·清洗·入黄金库 处理逻辑 (SOP)

> 版本 v1 · 2026-06-12 · 已逐项验证可行。本文件只记录"最终明确可行"的方案；中途证伪的做法不收录。
> 适用：把销售实发的开发/营销邮件，逐年找回正文 → 清洗脱敏 → 去重 → 归入三大场景 → 入库 → 人工评审(认可/不认可/需修改)。

---

## 0. 目标与三大场景

把所有"销售实发"的开发/营销邮件，识别归入以下 **三个场景之一**，入库后由人工判断是否认可：

| 场景 key | 场景名 | 含义 | 典型信号 |
|---|---|---|---|
| `re_activation` | 老客户激活 | 唤醒/问候已合作或久未联系的老客户 | 久未联系、好久不联系、再次联系、合作回顾、节日/新年问候、感谢一路相伴 |
| `new_business_promotion` | 老客户其他业务介绍 | 向老客户介绍新增/其他业务与成功案例 | 成功案例、新服务上线、还能为您做、业务拓展、案例分享 |
| `new_contact_intro` | 新客户业务介绍 | 向新客户/新接触人做公司与服务介绍 | 我是…来自事必达、初次/第一次联系、公司简介、诚挚问候+服务简介 |

**不属于这三类的一律拒绝入库**（见 §3 垃圾过滤），如：报价单、发票、交稿/发货、合同记录、纯回复线程、退信、操作类通知。

---

## 1. 数据来源与取数口径

- **正文权威源 = CRM 发件表 `spSendInfo{销售编号}`**（按销售 staff_id 分表，约 890 张非空分表）。**不要用跟进表 `usrCustomerFollowUpRecord`**（其正文字段为空，只有标签/活动日志）。
- **开发邮件只取"发送"，不取"收件"**：`WHERE MessageType='Email'`，按 `PlanSendTime` 取。
- **逐年递推**：以 `PlanSendTime` 按年（或自定义时间段）分批处理，便于增量与回溯。
- 关键字段：`SendId, PlanSendTime, Subject, EmailContent, EmlFtpDir`。
- 涉及哪些销售分表，可由跟进表 `RemarkClass LIKE '邮件宣传-%'` 的 `FollowUpStaff` 定位（也可全量遍历非空分表）。

## 2. 正文获取

1. `EmailContent`（ntext）**有值 → 直接用**（`Content` 列恒空，忽略）。
2. `EmailContent` 为空 → 按 `EmlFtpDir` 走 FTP 下载 `.eml` 解析：
   - `EmlFtpDir` = `年\月\日\uuid`，取前 3 段为日期。
   - FTP 凭据按日期查 `usrFtpTree`（`FtpType='文件'` + `StartDate/EndDate` 区间）得 host/user，再查 `usrFtpBackUp`（按 UserName）得 port/password；查不到回退全局 FTPHost。**不同时间段对应不同 FTP 服务器**。
   - 解析 `.eml` **只取当前这封正文**，剥离 `Re:` 之前的引用历史（切断标记：`发件人:`/`From:`/`-----Original Message-----`/`On … wrote:`/`在…写道:`/连续 `>` 引用块）。

## 3. 垃圾过滤（不入库）—— 关键，从严

**只要不是"主动群发的开发/营销外联"，一律拒绝。** 命中任一即丢弃：

### 3.1 系统/日志/退信
- `Admin:`、`跟进记录`、`File[PHONE]`、`□File`、`.txt/.docx/.xlsx`、`Working file`、`.pdf`、`||[时间]…`、`用途(` 开头、`couldn't be delivered`/`Out of office`(退信/自动回复)、邮件头 `x-sender`/`Message-ID`。

### 3.2 交易/报价/财务（强信号，最容易漏，务必拦）
- 正文含 `[AMOUNT]`(脱敏后的金额=报价)、`报价如下`、`报价:`、`报个价`、`单价`、`含税`、`成品尺寸`、`展开尺寸`、`页数:`、`框架协议`、`交货时间`、`交稿时间`、`账期`、`盖章`、独立 `PO`。
- 主题含 `报价`/`报个价`、`盖章`、`对账`、`财务报表`、`发票`、`注册证书`、`章程`、`PO金额`、`XS\d{3,}`(订单号)、`新工作计划`。
- 附件交付物：`请查收附件(合同/报价/发票/清单/List/目录/手册/稿/盖章)`、`附件…(合同/报价/发票/清单/List/盖章)`、`库存数量`。

### 3.3 生产/排期/数量（具体业务执行，非营销）
- **排期日期**：`\d{1,2}月\d{1,2}`、`\d{1,2}/\d{1,2}`、`\d{1,2}-\d{1,2}日`、`\d{1,2}月\d{1,2}-\d{1,2}`（如"3月26日同传"、"1月12-13日同传老师"）。
- **数量/规格**：`\d+(款|份|本|册|台|套|张|盒|箱|P|个)`、`\d+㎡`（如"6款宣传册各印500份"、"嘉法狮产品目录印刷(2000份)"）。
- **生产job**：`印刷$`/`印制$`/`排版印刷`/`印刷合同`/`各印`/`印\d+份`、`排版`、`汉化`、`名片`、具体产品名（控制器/组件/充电器/伸缩线/脂质辅料/摩托艇/光伏/养猪…）、`产品目录\d`/`目录\d{4}版`/`产品手册$`、`清单`/`库存`/`订单`/`增项`/`交稿`/`发货`/`物流`/`快递`/`加班`/`乔迁`。
- **过短/纯job 主题**：`^(设计|翻译|制作|印刷|排版|校对|同传|口译)$`。

### 3.4 回复/转发线程 与 其他（务必拦）
- **主题以 `Re:/RE:/Fw:/Fwd:/回复:/转发:/答复:` 开头的回复/转发线程一律拒绝**（正则：`^\s*(Re|Fw|Fwd|回复|转发|答复)\s*[:：]`）。营销群发都是首发，无此前缀；带前缀的是往来correspondence（询价/确认/合同/项目沟通），非营销。
- 操作动词主题：`PO查询`/`PO确认`/`询价`/`核对`/`确认$`/`费用与时间`/`时间评估`/`招标`、纯数字流水号(如 `Re: 231689`)。
- `11`/`邮件` 等无意义短主题；清洗后正文 `< 120` 字。

> 注意：**不要裸杀服务词**（笔译/口译/同传/翻译/印刷/展会/礼品/案例…）——它们在"服务介绍/案例分享/公司介绍"类营销邮件里是正常内容（如"交大事必达 – 专业翻译/口译/印刷一体化服务"、"事必达 同传案例分享"应保留）。拒绝只认 §3.2/§3.3/§3.4 的"交易/排期/数量/具体交付物/回复线程"信号。
> 经验：2025+2026 共 258 条"只按交易信号"重判后保留 123；2024 共 335 条再补"Re/Fw 线程"拒绝后保留 279。

## 4. 占位符规范化（去尖括号）

CRM 合并字段是尖括号占位符（如 `<ContactName>`），未转义会被前端当 HTML 标签吞掉、也是检索脏数据。统一替换为无尖括号标记：

| 原占位符(大小写/含空格忽略) | 替换 |
|---|---|
| ContactName / ContactEnglishName | `[客户称呼]` |
| StaffName / SenderName / SenderEnglishName | `[发件人]` |
| SenderMobile | `[手机]` ｜ SenderEmailAddress → `[邮箱]` |
| SenderExtendNo→`[分机]` ｜ SenderDivision→`[部门]` ｜ SenderCode→`[工号]` | |
| ContractId→`[合同号]` ｜ InputTime/PlanSendTime→`[日期]` | |
| `<[EMAIL]>` / `<mailto:[EMAIL]>` | `[EMAIL]` |
| 空 `<>`、16+位十六进制(Message-ID) | 删除 |
| 其他 CamelCase 合并字段 `<Xxx>` | `[字段]` |
| 装饰性 `< 文本 >` | 去尖括号保留文本 |

最后把残留的单个裸 `<`/`>` 中和（`<`→`‹`、`>`→`›`），保证产物 0 尖括号。

## 5. 脱敏

复用 `backend/export_mail_gold_candidates.py` 的正则，对正文+主题处理：
- `EMAIL_RE`→`[EMAIL]`、`URL_RE`→`[URL]`、`PHONE_RE`→`[PHONE]`、`MONEY_RE`→`[AMOUNT]`。
- **不要用 `LONG_ID_RE`**（会误伤英文长词）。
- **保留换行**（不要做整体空白折叠）。

### 5.1 人名脱敏（必做，正文+主题）
邮件正文/主题里残留大量人名，需脱敏：
- **收件人称呼 → `[客户称呼]`**：`^(Dear|Hi|Hello)\s+<名>`、`亲爱的/尊敬的<名>`（名非占位符时替换；保留问候词）。
- **发件人/销售实名 → `[发件人]`**：
  - 签名全名"中文姓名+英文名"：王慧莹 Tiffany、韩瑾 Angela、盛晔 Joyce、何珺 Alice、肖美鹏 David、赵文姣 Doris…（`[一-龥]{2,3}\s*<英文名>`）。
  - 发件语境英文名：`来自事必达/嘉赛/SPEED<名>`、`From <名> of Speed`、`事必达<名>为您/的问候`、`我是<名>`、`销售<名>`。
  - 中文销售名：`我是…事必达…的<名>`、`销售顾问<名>`、`(市场部/大客户部/销售部)的<名>`、确认的中文员工名（吴磊Leo、王尊安、丁涛、俞杭燕…）。
  - 核心员工名（签名确认：Tiffany/Angela/Joyce/Alice/David/Doris）**独立出现也脱敏**（称呼已先处理，余下即发件人）。
- **⚠️ 边界坑（务必）**：中文也是 `\w`，`\bTiffany\b` 在 CJK 紧邻时失效（"找Tiffany"/"David祝"无 `\b`）。**用 `(?<![A-Za-z])…(?![A-Za-z])`**（把中文当边界），否则 CJK 包围的英文名漏脱。
- 泄露的具体客户/对方公司名按需 → `贵司`（如 Giulia/Munters 已处理；ABB/巴斯夫等可后续按客户名表补脱）。
- 顺序：先称呼，再全名，再语境名，最后核心名独立脱敏；相邻重复 `[发件人][发件人]` 合并为一个。

## 6. 格式清洗

- **乱码**：删 `�`、`??`/`？？`、`?C`、`\xa0`→空格；明显错符如 `10000O`→`10000㎡`。
- **大段空行**：`\r\n`→`\n`；连续空行折叠为单空行；删纯空格行。
- **杂散符号行**：丢弃整行只有 `‹ › s • ·` 的行。
- **错误断行（一行被拆成多行）**：仅当"碎片化"时才激进重连——非空行中"≤4 字符短行占比 > 25%" 或 "平均行长 < 12"；重连规则：上一行不以句末标点(`。！？!?；;`)结尾、且当前行不是列表项时合并（中文直接接、英文加空格）。**本就段落正常的只清乱码/空行，保留原换行，不要乱并。**
- 标点前空格、数字间空格（如 `gifts .`、`路 1065 号`）归一。
- **HTML 邮件先转纯文本**：部分 EmailContent 是 Word/网页 HTML（`<P style=...>`/`MsoNormal`/`&nbsp;`/`<SPAN>`）。需在占位符规范化前 `html_to_text`：先**保护 `<占位符>`**（`<ContactName>` 等，用哨兵临时替换）→ 块级标签(`<p>/<br>/<div>/<tr>/<td>`)转换行、其余标签去除（任意长度）→ 解码实体(`&nbsp;`等)→ 还原占位符。否则 `<标签>` 会被当占位符吞掉、残留 `P style=`/`‹SPAN›` 乱码。检测：含 `<p/<span/<div/<table/MsoNormal/&nbsp;/style=`。

## 7. 去重（关键：按正文相似度，不是主题）

- 规范化正文用于比较：去 `[占位符]`、去称呼行(Dear/Hi/您好/亲爱的…)、去全部空白，取前 800 字。
- 用 `difflib.SequenceMatcher.ratio()`，**相似度 ≥ 0.90 视为同一封**（只差称呼/主题也算同一封）；并查集分组，每组保留正文最长/最完整 1 条。
- 同时与库内"已人工认可"的种子比对：与认可项重复的，删找回项、**认可项不动**。

## 8. 场景分类（归入三大场景或拒绝）

1. 有 `用途`(跟进表 `RemarkClass`) 时优先映射：
   - `邮件宣传-我们公司介绍` → `new_contact_intro`
   - `邮件宣传-节日问候`/`邮件宣传-双方合作历史` → `re_activation`
   - `邮件宣传-成功案例-*`/`本公司成功案例` → `new_business_promotion`
2. 无用途或需复核时，用正文/主题信号兜底（见 §0 信号词）。
3. 三类都不命中 → 不入库。

## 9. 入库（双写 + 待人工评审）

- **同时写入**：
  - `email_fragment_asset`（黄金库）：`source_type='mail_gold_seed'`、`function_fragment='full_email'`、`scenario_label/intent_label`=场景、`library_type='gold_seed'`、`useful_score=0.80`、`desensitized_status='desensitized'`、`source_snapshot` 记来源(`crm:spSendInfo`)/场景/批次标记。
  - `mail_raw_unified`（邮件原始数据）：`source_type='spsend_promo'`、`direction='outbound'`、`mail_uid` 用主题哈希去重。
- **编号**：`source_ref` 续接现有最大 `mail_gold_v2_NNN`。
- **状态**：入库即"未评审"；`allowed_for_generation/usable_for_reply/publishable=false`（脱敏完成但暂不放开 RAG 召回，待人工认可后再开）。
- **幂等**：按 `source_snapshot.email_stage` 批次标记 / `mail_uid` 防重复。

## 10. 人工评审与修改回路

人工在黄金库页面对每条打：**认可 / 不认可 / 需修改**（写入 `mail_gold_seed_review`，状态列显示，悬停看点评）。
- **需修改**：按 `review_comment` **仅改正文**（`content`），**状态及其他字段不变**。常见点评与处理：
  - "去掉时间信息"：删新年/马年/季节问候与 2025/2026 年份框架，保留营销主体。
  - "未脱敏"：补脱敏人名/客户公司名。
  - "格式乱/未分行"：按 §6 重排、服务点分行成项目列表。
  - "有乱字符"：按 §6 清乱码、改项目符号。
  - "只取当前段"：按 §2.2 剥离 Re 引用历史。
- 复核通过后再由人工/批量改为"认可"，并可放开 `allowed_for_generation` 进入 RAG。

---

## 11. 人工提出的问题 → 最终方案（速查）

| 人工问题 | 最终方案 |
|---|---|
| 正文找不到/跟进表是空的 | 正文在 `spSendInfo{staff}.EmailContent`；空则 FTP 拉 .eml（§1/§2） |
| 开发邮件该找哪条 | 只取发送(MessageType=Email)，不取收件；逐年 |
| FTP 不同时间段在哪 | 按 EmlFtpDir 日期查 `usrFtpTree/usrFtpBackUp`（§2.2） |
| .eml 含 Re 历史 | 只取当前段，剥离引用历史（§2.2） |
| 尖括号占位符导致显示空白/检索脏 | 占位符规范化为 `[…]`、中和裸尖括号（§4） |
| 去重不对（主题去重留了重复） | 改按正文相似度 ≥90% 去重（§7） |
| 大段空行 / 乱码 / 一行错误断成多行 | 格式清洗规则（§6） |
| 混入非营销（合同号/报价/Re 线程/退信） | 垃圾过滤丢弃（§3） |
| 需修改的怎么处理 | 按点评仅改正文，状态不变（§10） |
| 已认可的不要动 | 去重/清洗一律保护已认可项 |

---

## 12. 下一步（按本逻辑跑全量）

1. 逐年遍历 `spSendInfo` 全部非空分表，取 MessageType=Email 的发送邮件。
2. 依次执行 §2→§3→§4→§5→§6→§7→§8。
3. §9 双写入库（未评审）。
4. 交人工按 §10 评审；需修改回到 §10 处理；认可后放开召回。

---

## 13. 执行计划与进度表（按周）

> 范围：2026 + 2025 全部发件邮件，执行到 §10 入库（未评审、不放开召回）。§11 人工评审、§12 放开召回为后续，不阻塞本表。
> 节奏：按"发送月份周"切片，逐周处理。完成一周即勾选并回填"处理数/入库数/完成时间"。处理数=过滤+去重前扫描到的去重主题数；入库数=最终新增写入黄金库的条数。

说明：先跑 **2026**，再跑 **2025**；每年按月分周推进（一个自然月约 4–5 周切片）。已完成的批次在下表勾选并回填数字。

> 处理数 = 该月扫描去重(正文hash)后的唯一正文数；入库数 = 经场景命中 + 去重 + 对已入库去重 + **§3 从严复核**后最终**保留入库**黄金库的条数（操作/交易/生产类已剔除）。

### 2026 年
| 周/批次 | 发送时间范围 | 状态 | 处理数 | 入库数(复核后) | 完成时间 |
|---|---|:--:|--:|--:|---|
| 2026-W01 | 2026-01 | ☑ | 405 | 2 | 2026-06-12 |
| 2026-W02 | 2026-02 | ☑ | 230 | 3 | 2026-06-12 |
| 2026-W03 | 2026-03 | ☑ | 500 | 5 | 2026-06-12 |
| 2026-W04 | 2026-04 | ☑ | 435 | 18 | 2026-06-12 |
| 2026-W05 | 2026-05 | ☑ | 386 | 8 | 2026-06-12 |
| 2026-W06 | 2026-06 及以后 | ☑ | 204 | 3 | 2026-06-12 |
| **2026 小计** | | ☑ | **2160** | **39** | 2026-06-12 |

### 2025 年
| 周/批次 | 发送时间范围 | 状态 | 处理数 | 入库数(复核后) | 完成时间 |
|---|---|:--:|--:|--:|---|
| 2025-W01 | 2025-01 | ☑ | 544 | 7 | 2026-06-12 |
| 2025-W02 | 2025-02 | ☑ | 696 | 22 | 2026-06-12 |
| 2025-W03 | 2025-03 | ☑ | 738 | 9 | 2026-06-12 |
| 2025-W04 | 2025-04 | ☑ | 589 | 5 | 2026-06-12 |
| 2025-W05 | 2025-05 | ☑ | 531 | 6 | 2026-06-12 |
| 2025-W06 | 2025-06 | ☑ | 470 | 4 | 2026-06-12 |
| 2025-W07 | 2025-07 | ☑ | 603 | 7 | 2026-06-12 |
| 2025-W08 | 2025-08 | ☑ | 412 | 8 | 2026-06-12 |
| 2025-W09 | 2025-09 | ☑ | 460 | 4 | 2026-06-12 |
| 2025-W10 | 2025-10 | ☑ | 422 | 7 | 2026-06-12 |
| 2025-W11 | 2025-11 | ☑ | 470 | 3 | 2026-06-12 |
| 2025-W12 | 2025-12 | ☑ | 352 | 2 | 2026-06-12 |
| **2025 小计** | | ☑ | **6287** | **84** | 2026-06-12 |

### 2024 年（按新标准·从严过滤已内置）
| 周/批次 | 发送时间范围 | 状态 | 处理数 | 入库数(复核后) | 完成时间 |
|---|---|:--:|--:|--:|---|
| 2024-W01 | 2024-01 | ☑ | 1239 | 82 | 2026-06-12 |
| 2024-W02 | 2024-02 | ☑ | 750 | 31 | 2026-06-12 |
| 2024-W03 | 2024-03 | ☑ | 1142 | 16 | 2026-06-12 |
| 2024-W04 | 2024-04 | ☑ | 1144 | 23 | 2026-06-12 |
| 2024-W05 | 2024-05 | ☑ | 1010 | 34 | 2026-06-12 |
| 2024-W06 | 2024-06 | ☑ | 929 | 13 | 2026-06-12 |
| 2024-W07 | 2024-07 | ☑ | 1053 | 33 | 2026-06-12 |
| 2024-W08 | 2024-08 | ☑ | 863 | 20 | 2026-06-12 |
| 2024-W09 | 2024-09 | ☑ | 847 | 5 | 2026-06-12 |
| 2024-W10 | 2024-10 | ☑ | 732 | 2 | 2026-06-12 |
| 2024-W11 | 2024-11 | ☑ | 873 | 13 | 2026-06-12 |
| 2024-W12 | 2024-12 | ☑ | 814 | 7 | 2026-06-12 |
| **2024 小计** | | ☑ | **11396** | **279** | 2026-06-12 |

### 累计（全年份 2017–2026）
> 已逐年完成至 CRM 发件数据起点。**2016 及更早：spSendInfo 无 Email 数据（唯一正文=0），无可处理。**

| 年份 | 复核后保留入库 | 场景分布(激活/其他业务/新客户) | 状态 |
|---|--:|---|---|
| 2026 | 39 | 8 / 16 / 15 | ☑ |
| 2025 | 84 | 17 / 56 / 11 | ☑ |
| 2024 | 279 | 25 / 54 / 200 | ☑ |
| 2023 | 119 | 8 / 21 / 90 | ☑ |
| 2022 | 253 | 22 / 77 / 154 | ☑ |
| 2021 | 115 | 11 / 36 / 68 | ☑ |
| 2020 | 61 | 6 / 30 / 25 | ☑ |
| 2019 | 62 | 7 / 37 / 18 | ☑ |
| 2018 | 182 | 12 / 92 / 78 | ☑ |
| 2017 | 59 | 2 / 23 / 34 | ☑ |
| 2016–2010 | 0 | — | 无数据 |
| **合计** | **1253** | 116 / 442 / 695 | ☑ 完成 |

> 说明：
> - 已逐年(2017–2026)完成全部营销销售分表发件邮件按 SOP §1–§10 处理，每年跑完即校验脱敏+复合场景，**最终保留入库 1253 条**（已脱敏、未评审、未放开召回）。
> - 每年校验结果：尖括号 0 / 核心人名 0 / HTML 残留 0；操作类(报价/发票/印刷/排期/Re-Fw 等)已拒绝；少量正文内"[AMOUNT]"为举例金额非报价，保留。
> - 另有前期先行入库的"邮件宣传"开发邮件 110 条（34 认可批 + 76 找回批），已 §7 去重无重复。
> - 入库后 `email_fragment_asset` 的 `mail_gold_seed` 总数 = **1412**（49 原始切片种子 + 110 前期 + 1253 本轮）。
> - **§11 人工评审、§12 放开召回为后续**，不阻塞本表。
> - 复跑安全：按 `email_stage=whole_email_scenario_<年>` + 正文相似度去重，重复执行不重复入库。
