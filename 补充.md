下面给你整理成两段内容：

1. **以后每次在 Hermes 里发的完整启动任务指令**
2. **需要固定写进项目 md 文件里的规则和模板**

你以后可以按这个固定流程：

```
cd /mnt/d/items/QWhermes
```

进入 Hermes 后，把第 1 段完整复制进去。

---

# 一、Hermes 完整启动任务指令

这段是**发给 Hermes 的**，不是 WSL 命令。  
进入 `hermes` 聊天界面后复制发送。

```
请加载 codex skill。当前目录是 Git 项目。你的角色：你是本项目的任务主管、调度器和恢复器。Codex CLI 是代码执行员。你不要控制 Windows 的 Codex 插件、Codex 桌面版、VS Code 插件、Cursor、Antigravity 或任何图形界面。所有开发任务都必须通过当前 WSL 环境中的 Codex CLI 执行。请先读取以下内容：- AGENTS.md- TASKS.md- PROGRESS.md- TASK_HANDOFF.md- VALIDATION.md- git status- git diff- logs/codex-run.log- logs/codex-retry.log如果以下文件不存在，请先创建并初始化：- AGENTS.md- TASKS.md- PROGRESS.md- TASK_HANDOFF.md- VALIDATION.md- logs/codex-run.log- logs/codex-retry.log执行目标：请根据 TASKS.md 继续当前项目的第一个未完成任务。每次只处理一个小任务，不要一次性执行所有任务。如果 TASKS.md 中没有明确任务，请先根据当前项目文档、git diff 和 TASK_HANDOFF.md 整理任务清单，写入 TASKS.md，然后等待我确认，不要直接开始开发。Codex CLI 调用规则：1. 调用 Codex CLI 时优先使用：   codex exec --full-auto "任务内容"2. 工作目录必须是当前 Git 项目根目录。3. 不要使用 --dangerously-bypass-approvals-and-sandbox。4. 不要使用 danger-full-access。5. 不要跳过 Git 项目检查，除非只是做一次“请回复 ok”的临时测试。6. Codex CLI 只负责执行当前小任务，不要让它同时处理多个未完成任务。监控规则：1. Codex CLI 执行长任务时，请后台运行并持续监控。2. 每 2 分钟检查一次 Codex CLI 进程状态。3. 每 5 分钟读取一次 Codex 日志，并在当前 Hermes 对话中输出简短进展摘要。4. 每 5 分钟将进展追加写入 logs/codex-run.log。5. 如果 Codex 没有新输出，也要记录“仍在运行，无新日志”。6. 每完成一个小点，必须更新 PROGRESS.md，并记录完成时间。7. 每完成一个任务，必须更新 TASKS.md、PROGRESS.md 和 TASK_HANDOFF.md。Token / rate limit 处理规则：1. 如果 Codex CLI 返回 token 不足、rate limit、quota、temporarily unavailable、429、usage limit 或类似限制错误，不要放弃任务。2. 将错误时间、错误内容、当前任务、下一步写入 TASK_HANDOFF.md。3. 将本次失败追加到 logs/codex-retry.log。4. 每 30 分钟尝试一次 Codex CLI 是否恢复。5. 每次重试前先读取 TASKS.md、PROGRESS.md、TASK_HANDOFF.md、git status 和 git diff。6. 如果恢复，则继续当前第一个未完成任务。7. 如果连续失败 6 次，停止继续重试，记录需要人工处理。意外停止处理规则：1. 如果 Codex CLI 进程异常退出，请读取最近日志、git status 和 git diff。2. 更新 TASK_HANDOFF.md，记录中断时间、中断原因、已完成内容、未完成内容和下一步。3. 如果是普通中断，请尝试恢复当前小任务。4. 如果是 token/rate limit，请进入 30 分钟重试策略。5. 如果是登录失败、需要人工授权、危险命令、权限不足或未知异常，请停止并记录原因，不要自动继续。验证规则：1. Codex 完成当前小任务后，先检查 git diff。2. 按 VALIDATION.md 中的验证命令执行验证。3. 验证通过后，更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。4. 验证失败时，记录失败原因和下一步建议，不要继续下一个任务。5. 如果所有任务完成，请运行最终验证。安全规则：1. 不要自动推送远程仓库。2. 不要删除大量文件。3. 不要修改生产环境配置。4. 不要访问、输出、保存密钥、密码、token。5. 不要执行数据库写入、清库、迁移等高风险操作，除非我明确确认。6. 不要安装未知来源依赖。7. 遇到需要人工授权的高风险操作时，停止并写入 TASK_HANDOFF.md。完成规则：如果 TASKS.md 中所有任务都完成，请执行：1. 运行 VALIDATION.md 中的所有最终验证命令。2. 更新 TASKS.md，确认所有任务已完成。3. 更新 PROGRESS.md，记录最终完成时间。4. 生成 FINAL_REPORT.md。5. FINAL_REPORT.md 必须包含：   - 完成时间   - 完成任务列表   - 修改文件列表   - 验证命令   - 验证结果   - 未解决风险   - 建议下一步6. 在 logs/codex-run.log 末尾追加：   ALL TASKS COMPLETED: 当前时间7. 停止继续调用 Codex CLI，等待我确认。现在请先读取项目状态，判断第一个未完成任务，并开始执行。
```

---

# 二、固定写进 md 文件中的内容

这些是**放在项目根目录的 md 文件**。以后所有项目建议都统一这几个文件名：

```
AGENTS.mdTASKS.mdPROGRESS.mdTASK_HANDOFF.mdVALIDATION.mdFINAL_REPORT.mdlogs/codex-run.loglogs/codex-retry.log
```

## 1）AGENTS.md：长期规则

这是最重要的文件，建议固定放这些内容。

```
# AGENTS## 角色分工- Hermes 是本项目的任务主管、调度器和恢复器。- Codex CLI 是代码执行员。- 不控制 Windows 的 Codex 插件、Codex 桌面版、VS Code 插件、Cursor、Antigravity 或任何图形界面。- 所有自动化开发任务都通过当前环境中的 Codex CLI 执行。## 固定读取文件所有 Agent 开始工作前必须先读取：- AGENTS.md- TASKS.md- PROGRESS.md- TASK_HANDOFF.md- VALIDATION.md- git status- git diff- logs/codex-run.log- logs/codex-retry.log## Codex CLI 调用规则- 默认使用：```bashcodex exec --full-auto "任务内容"
```

- 工作目录必须是当前 Git 项目根目录。
- 不使用：

```
--dangerously-bypass-approvals-and-sandbox
```

- 不使用：

```
--sandbox danger-full-access
```

- 不跳过 Git 项目检查，除非只是临时测试。
- 每次只让 Codex CLI 执行一个小任务。

## 任务执行规则

- 每次只处理 TASKS.md 中第一个未完成任务。
- 不要一次性完成所有任务。
- 每完成一个小点，必须更新 PROGRESS.md。
- 每完成一个任务，必须更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。
- 修改代码前必须查看 git status。
- 修改代码后必须查看 git diff。
- 修改完成后必须执行 VALIDATION.md 中的验证命令。

## 进度记录规则

PROGRESS.md 必须记录：

- 当前任务
- 当前小点
- 状态
- 最近更新时间
- 最近完成的小点
- 完成时间
- 当前卡点
- 下一步
- 最近验证结果

每次调用 Codex CLI 前后，都要追加 logs/codex-run.log。

## 运行监控规则

- Hermes 调用 Codex CLI 执行长任务时，必须后台运行并监控。
- Codex CLI 长任务运行时，每 2 分钟检查一次进程状态。
- 每 5 分钟读取一次 Codex 日志。
- 每 5 分钟将当前状态追加到 logs/codex-run.log。
- 如果 Codex 没有新输出，也要记录“仍在运行，无新日志”。
- 如果 Codex CLI 进程异常退出：
  1. 读取最近日志。
  2. 查看 git status 和 git diff。
  3. 更新 TASK_HANDOFF.md。
  4. 判断是否可恢复。
  5. 如果是普通中断，重新调用 Codex CLI 继续当前小任务。
  6. 如果是 token/rate limit，进入 30 分钟重试策略。
  7. 如果是授权、登录、危险命令、权限不足或未知异常，停止并等待人工处理。

## Token / Rate Limit 处理规则

如果 Codex CLI 返回以下错误：

- token 不足
- rate limit
- quota
- temporarily unavailable
- 429
- usage limit
- 模型额度不足
- 上下文不足

处理规则：

1. 不要放弃任务。
2. 将失败原因、时间、当前任务、下一步写入 TASK_HANDOFF.md。
3. 将本次失败追加到 logs/codex-retry.log。
4. 每 30 分钟尝试一次 Codex CLI 是否恢复。
5. 每次重试前先读取 TASKS.md、PROGRESS.md、TASK_HANDOFF.md、git status 和 git diff。
6. 恢复后继续 TASKS.md 中第一个未完成任务。
7. 如果连续失败 6 次，停止并记录需要人工处理。

## 验证规则

- Codex 完成当前小任务后，先检查 git diff。
- 按 VALIDATION.md 中的验证命令执行验证。
- 验证通过后，更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。
- 验证失败时，记录失败原因和下一步建议，不要继续下一个任务。

## 安全规则

禁止自动执行以下操作：

- 推送远程仓库
- 删除大量文件
- 修改生产环境配置
- 输出密钥、密码、token
- 保存密钥、密码、token
- 数据库写入
- 清库
- 生产数据库迁移
- 安装未知来源依赖
- 修改系统级目录

遇到以上操作，必须停止并写入 TASK_HANDOFF.md，等待人工确认。

## 完成规则

如果 TASKS.md 中所有任务都完成：

1. 运行 VALIDATION.md 中的所有最终验证命令。
2. 更新 TASKS.md。
3. 更新 PROGRESS.md，记录最终完成时间。
4. 生成 FINAL_REPORT.md。
5. FINAL_REPORT.md 必须包含：
   - 完成时间
   - 完成任务列表
   - 修改文件列表
   - 验证命令
   - 验证结果
   - 未解决风险
   - 建议下一步
6. 在 logs/codex-run.log 末尾追加：

```
ALL TASKS COMPLETED: 当前时间
```

7. 停止继续调用 Codex CLI，等待人工确认。

```
---## 2）TASKS.md：任务清单模板```markdown# TASKS## 当前大目标在这里写当前大任务目标。例如：完成 30 页项目文档对应的代码开发、验证、修复和最终报告。## 任务清单- [ ] Task 1：- [ ] Task 2：- [ ] Task 3：- [ ] Task 4：- [ ] Task 5：## 任务执行规则- 每次只执行第一个未完成任务。- 每个任务完成后必须更新本文件。- 不允许跳过未完成任务。- 如果任务需要拆分，请在本文件中新增子任务。
```

---

## 3）PROGRESS.md：进度文件模板

```
# PROGRESS## 当前状态- 当前任务：- 当前小点：- 状态：- 最近更新时间：## 最近完成的小点| 时间 | 任务 | 小点 | 结果 | 验证 ||---|---|---|---|---||  |  |  |  |  |## 已完成- 无## 当前卡点- 无## 下一步- 读取 TASKS.md，继续第一个未完成任务。## 最近验证- 尚未验证。
```

---

## 4）TASK_HANDOFF.md：交接/恢复文件模板

```
# TASK_HANDOFF## 当前目标待补充。## 当前任务待补充。## 已完成内容- 待补充。## 未完成内容- 待补充。## 已修改文件- 待补充。## 当前卡点- 无。## 最近中断记录| 时间 | 类型 | 原因 | 当前任务 | 下一步 ||---|---|---|---|---||  |  |  |  |  |## Token / Rate Limit 记录| 时间 | 错误类型 | 错误内容 | 重试次数 | 下一次重试 ||---|---|---|---|---||  |  |  |  |  |## 下一步建议- 读取 TASKS.md 中第一个未完成任务并继续。## 注意事项- 不要删除现有数据文件。- 不要覆盖人工整理文档。- 不要自动推送远程仓库。- 不要修改生产环境配置。
```

---

## 5）VALIDATION.md：验证规则模板

这个根据项目类型调整。

### 通用最小版

```
# VALIDATION## 验证命令```bashgit diff --check
```

## 通过标准

- 命令返回 0。
- 没有明显语法错误。
- 关键输出文件存在。
- PROGRESS.md 已更新。
- TASK_HANDOFF.md 已更新。

```
### Node.js 项目```markdown# VALIDATION## 验证命令```bashgit diff --checknpm testnpm run lintnpm run build
```

## 通过标准

- 所有命令返回 0。
- 测试通过。
- lint 通过。
- build 通过。
- PROGRESS.md 已更新。
- TASK_HANDOFF.md 已更新。

```
### Python 项目```markdown# VALIDATION## 验证命令```bashgit diff --checkpython -m pytest
```

## 通过标准

- 所有命令返回 0。
- pytest 通过。
- 没有明显语法错误。
- PROGRESS.md 已更新。
- TASK_HANDOFF.md 已更新。

```
---## 6）logs 文件这两个不是 md，但建议固定创建：```bashmkdir -p logstouch logs/codex-run.logtouch logs/codex-retry.log
```

作用：

```
logs/codex-run.log      记录 Codex 执行过程、进展、完成时间logs/codex-retry.log    记录 token/rate limit/失败重试记录
```

---

# 三、一次性创建这些文件的命令

进入项目根目录后执行：

```
mkdir -p logstouch AGENTS.mdtouch TASKS.mdtouch PROGRESS.mdtouch TASK_HANDOFF.mdtouch VALIDATION.mdtouch logs/codex-run.logtouch logs/codex-retry.log
```

如果你要直接写入完整模板，可以手动复制上面内容分别保存。

---

# 四、以后查看进度的命令

新开一个 WSL 窗口，进入项目目录：

```
cd /mnt/d/items/QW
```

看当前进度：

```
cat PROGRESS.md
```

看任务清单：

```
cat TASKS.md
```

看是否卡住：

```
cat TASK_HANDOFF.md
```

看最近日志：

```
tail -n 100 logs/codex-run.log
```

实时看日志：

```
tail -f logs/codex-run.log
```

看重试记录：

```
tail -n 100 logs/codex-retry.log
```

看是否全部完成：

```
grep -n "ALL TASKS COMPLETED" logs/codex-run.log
```

---

# 五、最简日常用法

以后你的操作就是：

```
cd /mnt/d/items/QWhermes
```

然后把“第一段 Hermes 完整启动任务指令”发进去。

如果只是想看进度，在 Hermes 里发：

```
请读取 PROGRESS.md、TASKS.md、TASK_HANDOFF.md、logs/codex-run.log、logs/codex-retry.log、git status 和 git diff。请告诉我：1. 当前总进度2. 第一个未完成任务3. 上一个完成的小点是什么4. 上一个完成小点的时间5. 当前是否有卡点6. 如果中断，应该从哪里继续7. 最近一次验证是否通过
```

一句话总结：**启动任务指令每次发给 Hermes；固定规则写进 AGENTS.md；实时状态写进 PROGRESS.md / TASK_HANDOFF.md / logs；验证命令写进 VALIDATION.md。**

指令会太长吗，不应该 写入对应md文件，还有这么多内容hermes会完整执行不漏吗

已思考若干秒

对，你这个判断是对的：**启动指令太长，不应该每次都把全部规则塞给 Hermes。**

更稳的做法是：

```
长期固定规则 → 写进 AGENTS.md任务清单 → 写进 TASKS.md当前进度 → 写进 PROGRESS.md中断恢复 → 写进 TASK_HANDOFF.md验证命令 → 写进 VALIDATION.md每次启动 Hermes → 只发一段短指令，让它读取这些文件并执行
```

这样 Hermes 更不容易漏，而且每次中断恢复时也有文件依据。

---

## 推荐最终结构

以后项目根目录固定保留这些文件：

```
AGENTS.md          固定规则，最重要TASKS.md           任务清单PROGRESS.md        当前进度TASK_HANDOFF.md    中断恢复和交接信息VALIDATION.md      验证命令logs/codex-run.loglogs/codex-retry.logFINAL_REPORT.md    全部完成后生成
```

**AGENTS.md 里放长规则。**  
**Hermes 启动时只发短指令。**

---

# 一、以后发给 Hermes 的短启动指令

进入项目目录后：

```
cd /mnt/d/items/QWhermes
```

然后在 Hermes 里只发这段：

```
请加载 codex skill。当前目录是 Git 项目。请先读取 AGENTS.md、TASKS.md、PROGRESS.md、TASK_HANDOFF.md、VALIDATION.md、git status、git diff 和 logs 目录。你作为任务主管，通过 Codex CLI 执行任务，不要控制 Windows 的 Codex 插件、桌面版或任何图形界面。请严格按 AGENTS.md 的规则执行：1. 只处理 TASKS.md 中第一个未完成任务。2. 调用 Codex CLI 时默认使用 --full-auto。3. 执行中持续监控并写入 logs/codex-run.log。4. 每完成一个小点，更新 PROGRESS.md 并记录时间。5. 完成任务后运行 VALIDATION.md 中的验证命令。6. 验证通过后更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。7. 如果 token/rate limit，按 AGENTS.md 规则每 30 分钟重试。8. 如果登录失败、需要人工授权、危险命令或未知异常，停止并写入 TASK_HANDOFF.md。现在开始执行第一个未完成任务。
```

这个长度就够了。

---

# 二、写进 AGENTS.md 的固定规则

你可以把下面内容放进项目根目录的 `AGENTS.md`。以后每个项目都用这份。

```
# AGENTS## 角色分工- Hermes 是任务主管、调度器和恢复器。- Codex CLI 是代码执行员。- 不控制 Windows 的 Codex 插件、Codex 桌面版、VS Code 插件、Cursor、Antigravity 或任何图形界面。- 所有自动化开发任务都通过当前环境中的 Codex CLI 执行。## 启动时必须读取每次开始工作前必须读取：- AGENTS.md- TASKS.md- PROGRESS.md- TASK_HANDOFF.md- VALIDATION.md- git status- git diff- logs/codex-run.log- logs/codex-retry.log## Codex CLI 调用规则默认使用：```bashcodex exec --full-auto "任务内容"
```

规则：

- 工作目录必须是当前 Git 项目根目录。
- 不使用 `--dangerously-bypass-approvals-and-sandbox`。
- 不使用 `--sandbox danger-full-access`。
- 不跳过 Git 项目检查，除非只是临时测试。
- 每次只让 Codex CLI 执行一个小任务。
- 不要让 Codex CLI 同时处理多个未完成任务。

## 任务执行规则

- 每次只处理 TASKS.md 中第一个未完成任务。
- 不要一次性完成所有任务。
- 修改代码前必须查看 git status。
- 修改代码后必须查看 git diff。
- 每完成一个小点，必须更新 PROGRESS.md，并记录完成时间。
- 每完成一个任务，必须更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。
- 修改完成后必须执行 VALIDATION.md 中的验证命令。

## 进度记录规则

PROGRESS.md 必须记录：

- 当前任务
- 当前小点
- 状态
- 最近更新时间
- 最近完成的小点
- 完成时间
- 当前卡点
- 下一步
- 最近验证结果

每次调用 Codex CLI 前后，都要追加 logs/codex-run.log。

## 运行监控规则

- Codex CLI 长任务必须后台运行并监控。
- 每 2 分钟检查一次 Codex CLI 进程状态。
- 每 5 分钟读取一次 Codex 日志。
- 每 5 分钟将当前状态追加到 logs/codex-run.log。
- 如果 Codex 没有新输出，也要记录“仍在运行，无新日志”。

如果 Codex CLI 进程异常退出：

1. 读取最近日志。
2. 查看 git status 和 git diff。
3. 更新 TASK_HANDOFF.md。
4. 判断是否可恢复。
5. 如果是普通中断，继续当前小任务。
6. 如果是 token/rate limit，进入 30 分钟重试策略。
7. 如果是授权、登录、危险命令、权限不足或未知异常，停止并等待人工处理。

## Token / Rate Limit 处理规则

如果 Codex CLI 返回以下错误：

- token 不足
- rate limit
- quota
- temporarily unavailable
- 429
- usage limit
- 模型额度不足
- 上下文不足

处理规则：

1. 不要放弃任务。
2. 将失败原因、时间、当前任务、下一步写入 TASK_HANDOFF.md。
3. 将本次失败追加到 logs/codex-retry.log。
4. 每 30 分钟尝试一次 Codex CLI 是否恢复。
5. 每次重试前读取 TASKS.md、PROGRESS.md、TASK_HANDOFF.md、git status 和 git diff。
6. 恢复后继续 TASKS.md 中第一个未完成任务。
7. 如果连续失败 6 次，停止并记录需要人工处理。

## 验证规则

- Codex 完成当前小任务后，先检查 git diff。
- 按 VALIDATION.md 中的验证命令执行验证。
- 验证通过后，更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。
- 验证失败时，记录失败原因和下一步建议，不要继续下一个任务。

## 安全规则

禁止自动执行以下操作：

- 推送远程仓库
- 删除大量文件
- 修改生产环境配置
- 输出密钥、密码、token
- 保存密钥、密码、token
- 数据库写入
- 清库
- 生产数据库迁移
- 安装未知来源依赖
- 修改系统级目录

遇到以上操作，必须停止并写入 TASK_HANDOFF.md，等待人工确认。

## 完成规则

如果 TASKS.md 中所有任务都完成：

1. 运行 VALIDATION.md 中的所有最终验证命令。
2. 更新 TASKS.md。
3. 更新 PROGRESS.md，记录最终完成时间。
4. 生成 FINAL_REPORT.md。
5. FINAL_REPORT.md 必须包含：
   - 完成时间
   - 完成任务列表
   - 修改文件列表
   - 验证命令
   - 验证结果
   - 未解决风险
   - 建议下一步
6. 在 logs/codex-run.log 末尾追加：

```
ALL TASKS COMPLETED: 当前时间
```

7. 停止继续调用 Codex CLI，等待人工确认。

```
---# 三、其它 md 文件放什么？## `TASKS.md````markdown# TASKS## 当前大目标在这里写当前大任务目标。## 任务清单- [ ] Task 1：- [ ] Task 2：- [ ] Task 3：- [ ] Task 4：- [ ] Task 5：## 任务执行规则- 每次只执行第一个未完成任务。- 每个任务完成后必须更新本文件。- 如果任务需要拆分，请在本文件中新增子任务。
```

## `PROGRESS.md`

```
# PROGRESS## 当前状态- 当前任务：- 当前小点：- 状态：- 最近更新时间：## 最近完成的小点| 时间 | 任务 | 小点 | 结果 | 验证 ||---|---|---|---|---||  |  |  |  |  |## 已完成- 无## 当前卡点- 无## 下一步- 读取 TASKS.md，继续第一个未完成任务。## 最近验证- 尚未验证。
```

## `TASK_HANDOFF.md`

```
# TASK_HANDOFF## 当前目标待补充。## 当前任务待补充。## 已完成内容- 待补充。## 未完成内容- 待补充。## 已修改文件- 待补充。## 当前卡点- 无。## 最近中断记录| 时间 | 类型 | 原因 | 当前任务 | 下一步 ||---|---|---|---|---||  |  |  |  |  |## Token / Rate Limit 记录| 时间 | 错误类型 | 错误内容 | 重试次数 | 下一次重试 ||---|---|---|---|---||  |  |  |  |  |## 下一步建议- 读取 TASKS.md 中第一个未完成任务并继续。## 注意事项- 不要删除现有数据文件。- 不要覆盖人工整理文档。- 不要自动推送远程仓库。- 不要修改生产环境配置。
```

## `VALIDATION.md`

先用最小版：

```
# VALIDATION## 验证命令```bashgit diff --check
```

## 通过标准

- 命令返回 0。
- 没有明显语法错误。
- 关键输出文件存在。
- PROGRESS.md 已更新。
- TASK_HANDOFF.md 已更新。

```
如果是 Node 项目，再改成：```markdown# VALIDATION## 验证命令```bashgit diff --checknpm testnpm run lintnpm run build
```

## 通过标准

- 所有命令返回 0。
- 测试通过。
- lint 通过。
- build 通过。
- PROGRESS.md 已更新。
- TASK_HANDOFF.md 已更新。

```
如果是 Python 项目：```markdown# VALIDATION## 验证命令```bashgit diff --checkpython -m pytest
```

## 通过标准

- 所有命令返回 0。
- pytest 通过。
- 没有明显语法错误。
- PROGRESS.md 已更新。
- TASK_HANDOFF.md 已更新。

```
---## 四、这样 Hermes 会不会完整执行不漏？不能保证 100% 不漏，但这样设计是为了**最大限度降低漏执行概率**。更稳的原因是：```text1. 长规则不靠一次聊天记忆，而是写在 AGENTS.md2. 当前任务不靠 Hermes 猜，而是写在 TASKS.md3. 当前进度不靠终端输出，而是写在 PROGRESS.md4. 中断恢复不靠上一轮上下文，而是写在 TASK_HANDOFF.md5. 验证不靠模型主观判断，而是写在 VALIDATION.md6. 日志可追踪，失败后能恢复
```

最关键的是：**不要把全部规则塞在一次长提示里，而是让 Hermes 每次先读文件。**

---

## 五、最推荐的实际用法

第一次项目初始化时：

```
cd /mnt/d/items/QWmkdir -p logstouch AGENTS.md TASKS.md PROGRESS.md TASK_HANDOFF.md VALIDATION.md logs/codex-run.log logs/codex-retry.log
```

把上面的模板填进去。

以后每天使用只需要：

```
cd /mnt/d/items/QWhermes
```

然后发短指令：

```
请加载 codex skill。当前目录是 Git 项目。请先读取 AGENTS.md、TASKS.md、PROGRESS.md、TASK_HANDOFF.md、VALIDATION.md、git status、git diff 和 logs 目录。你作为任务主管，通过 Codex CLI 执行任务，不要控制 Windows 的 Codex 插件、桌面版或任何图形界面。请严格按 AGENTS.md 的规则执行，只处理 TASKS.md 中第一个未完成任务。调用 Codex CLI 时默认使用 --full-auto。完成后运行 VALIDATION.md 中的验证命令并更新进度。如果 token/rate limit，则按 AGENTS.md 规则每 30 分钟重试。如果登录失败、需要人工授权、危险命令或未知异常，停止并写入 TASK_HANDOFF.md。现在开始执行第一个未完成任务。
```

这个才是最终推荐版本。

请把最关键，确保流程能顺利运行，重试的放在第一要位，包括在指令中要明确

已思考 1 秒

对，这次应该把**“不中断、可恢复、自动重试”放第一优先级**，其次才是执行任务、验证、安全规则。

下面给你最终版，分两部分：

1. **发给 Hermes 的短启动指令：每次启动 Hermes 后复制这段**
2. **写进 `AGENTS.md` 的固定规则：一次写入，长期使用**

---

# 一、每次发给 Hermes 的启动指令：重试优先版

进入项目目录后：

```
cd /mnt/d/items/QWhermes
```

然后在 Hermes 里发：

```
请加载 codex skill。当前目录是 Git 项目。最高优先级：确保任务可恢复、可重试、不中断。不要因为 Codex CLI token 不足、rate limit、quota、429、usage limit、temporarily unavailable、网络临时错误、进程意外退出而放弃任务。请先读取：- AGENTS.md- TASKS.md- PROGRESS.md- TASK_HANDOFF.md- VALIDATION.md- git status- git diff- logs/codex-run.log- logs/codex-retry.log如果上述文件或 logs 目录不存在，请先创建并初始化。你作为任务主管，通过 Codex CLI 执行任务，不要控制 Windows 的 Codex 插件、桌面版、VS Code 插件、Cursor、Antigravity 或任何图形界面。请严格按 AGENTS.md 的规则执行，尤其优先执行其中的“恢复与重试规则”。执行要求：1. 只处理 TASKS.md 中第一个未完成任务。2. 调用 Codex CLI 时默认使用 --full-auto。3. Codex CLI 长任务必须后台运行并监控。4. 每 2 分钟检查一次 Codex CLI 进程状态。5. 每 5 分钟将进展追加到 logs/codex-run.log，并更新 PROGRESS.md。6. 每完成一个小点，必须记录完成时间。7. 如果 Codex CLI 发生 token 不足、rate limit、quota、429、usage limit、temporarily unavailable 或网络临时错误：   - 不要放弃任务。   - 立即更新 TASK_HANDOFF.md。   - 追加 logs/codex-retry.log。   - 每 30 分钟重试一次。   - 每次重试前重新读取 TASKS.md、PROGRESS.md、TASK_HANDOFF.md、git status 和 git diff。   - 如果恢复，则继续当前未完成任务。   - 连续失败 6 次后才停止，并记录需要人工处理。8. 如果 Codex CLI 进程意外退出：   - 读取最近日志和 git diff。   - 更新 TASK_HANDOFF.md。   - 判断是否可恢复。   - 如果是普通中断，继续当前小任务。   - 如果是 token/rate limit，进入 30 分钟重试。9. 如果遇到登录失败、需要人工授权、危险命令、权限不足、删除大量文件、推送远程仓库、修改生产环境配置、访问密钥、数据库写操作，请停止并写入 TASK_HANDOFF.md，不要自动继续。10. 当前小任务完成后，运行 VALIDATION.md 中的验证命令。11. 验证通过后，更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。12. 如果所有任务完成，生成 FINAL_REPORT.md，并在 logs/codex-run.log 末尾追加 ALL TASKS COMPLETED: 当前时间。现在开始执行第一个未完成任务。第一目标是保证任务持续推进和可恢复，其次才是完成代码修改。
```

---

# 二、写进 `AGENTS.md` 的固定规则：重试优先版

把下面内容写入项目根目录的：

```
AGENTS.md
```

以后每个项目都可以用这份。

```
# AGENTS## 0. 最高优先级：恢复、重试、不中断本项目的最高优先级不是一次性快速完成，而是确保任务可恢复、可重试、不中断。如果 Codex CLI 出现以下情况，不允许直接放弃任务：- token 不足- rate limit- quota- temporarily unavailable- 429- usage limit- 模型额度不足- 上下文不足- 网络临时错误- Codex CLI 进程意外退出- 终端会话中断- 长任务无输出但进程仍存在处理原则：1. 先记录，再判断，再恢复。2. 所有失败必须写入 TASK_HANDOFF.md。3. 所有重试必须写入 logs/codex-retry.log。4. 所有运行进展必须写入 logs/codex-run.log。5. 每次恢复前必须重新读取 TASKS.md、PROGRESS.md、TASK_HANDOFF.md、git status 和 git diff。6. 不要依赖上一次聊天记忆恢复任务，必须依赖项目文件和 Git 状态恢复。## 1. 角色分工- Hermes 是任务主管、调度器和恢复器。- Codex CLI 是代码执行员。- 不控制 Windows 的 Codex 插件、Codex 桌面版、VS Code 插件、Cursor、Antigravity 或任何图形界面。- 所有自动化开发任务都通过当前环境中的 Codex CLI 执行。## 2. 启动时必须读取每次开始工作前必须读取：- AGENTS.md- TASKS.md- PROGRESS.md- TASK_HANDOFF.md- VALIDATION.md- git status- git diff- logs/codex-run.log- logs/codex-retry.log如果文件不存在，必须先创建并初始化。## 3. Codex CLI 调用规则默认使用：```bashcodex exec --full-auto "任务内容"
```

规则：

- 工作目录必须是当前 Git 项目根目录。
- 不使用 `--dangerously-bypass-approvals-and-sandbox`。
- 不使用 `--sandbox danger-full-access`。
- 不跳过 Git 项目检查，除非只是临时测试。
- 每次只让 Codex CLI 执行一个小任务。
- 不要让 Codex CLI 同时处理多个未完成任务。

## 4. 运行监控规则

Codex CLI 长任务必须后台运行并监控。

监控频率：

- 每 2 分钟检查一次 Codex CLI 进程状态。
- 每 5 分钟读取一次 Codex 日志。
- 每 5 分钟将当前状态追加到 logs/codex-run.log。
- 每 5 分钟更新 PROGRESS.md。
- 如果 Codex 没有新输出，也要记录“仍在运行，无新日志”。

logs/codex-run.log 每条记录必须包含：

- 时间
- 当前任务
- 当前小点
- Codex CLI 状态
- 最近输出摘要
- 下一步动作

## 5. Token / Rate Limit 自动重试规则

如果 Codex CLI 返回以下错误：

- token 不足
- rate limit
- quota
- temporarily unavailable
- 429
- usage limit
- 模型额度不足
- 上下文不足
- 网络临时错误

必须执行以下流程：

1. 不要放弃任务。
2. 立即记录当前时间、错误类型、错误内容、当前任务、当前 git diff 摘要。
3. 更新 TASK_HANDOFF.md。
4. 追加 logs/codex-retry.log。
5. 每 30 分钟尝试一次 Codex CLI 是否恢复。
6. 每次重试前必须重新读取：
   - TASKS.md
   - PROGRESS.md
   - TASK_HANDOFF.md
   - git status
   - git diff
   - logs/codex-retry.log
7. 如果恢复，继续 TASKS.md 中第一个未完成任务。
8. 如果连续失败 6 次，停止继续重试，并在 TASK_HANDOFF.md 中记录需要人工处理。
9. 停止后不要清理临时文件，不要回滚代码，等待人工确认。

logs/codex-retry.log 每条记录必须包含：

- 时间
- 错误类型
- 错误内容
- 当前任务
- 重试次数
- 下一次重试时间
- 是否恢复

## 6. Codex CLI 意外停止恢复规则

如果 Codex CLI 进程异常退出、终端中断、无结果返回或长任务停止：

1. 读取最近 logs/codex-run.log。
2. 查看 git status。
3. 查看 git diff。
4. 更新 TASK_HANDOFF.md。
5. 判断已完成内容和未完成内容。
6. 如果是普通中断，重新调用 Codex CLI 继续当前小任务。
7. 如果是 token/rate limit，进入 30 分钟重试策略。
8. 如果是登录失败、授权、危险命令、权限不足或未知异常，停止并等待人工处理。

## 7. 任务执行规则

- 每次只处理 TASKS.md 中第一个未完成任务。
- 不要一次性完成所有任务。
- 修改代码前必须查看 git status。
- 修改代码后必须查看 git diff。
- 每完成一个小点，必须更新 PROGRESS.md，并记录完成时间。
- 每完成一个任务，必须更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。
- 修改完成后必须执行 VALIDATION.md 中的验证命令。

## 8. 进度记录规则

PROGRESS.md 必须记录：

- 当前任务
- 当前小点
- 状态
- 最近更新时间
- 最近完成的小点
- 完成时间
- 当前卡点
- 下一步
- 最近验证结果

每完成一个小点，必须追加一条完成记录。

## 9. 验证规则

- Codex 完成当前小任务后，先检查 git diff。
- 按 VALIDATION.md 中的验证命令执行验证。
- 验证通过后，更新 TASKS.md、PROGRESS.md、TASK_HANDOFF.md。
- 验证失败时，记录失败原因和下一步建议，不要继续下一个任务。
- 验证失败不是任务结束，必须记录恢复建议。

## 10. 安全停止规则

遇到以下情况必须停止，不得自动继续：

- 登录失败
- 需要人工授权
- 删除大量文件
- 推送远程仓库
- 修改生产环境配置
- 输出密钥、密码、token
- 保存密钥、密码、token
- 数据库写入
- 清库
- 生产数据库迁移
- 安装未知来源依赖
- 修改系统级目录
- 权限不足且无法判断风险

停止时必须：

1. 更新 TASK_HANDOFF.md。
2. 追加 logs/codex-run.log。
3. 写明停止原因。
4. 写明需要人工确认的事项。
5. 不要回滚当前改动，除非用户明确要求。

## 11. 完成规则

如果 TASKS.md 中所有任务都完成：

1. 运行 VALIDATION.md 中的所有最终验证命令。
2. 更新 TASKS.md。
3. 更新 PROGRESS.md，记录最终完成时间。
4. 生成 FINAL_REPORT.md。
5. FINAL_REPORT.md 必须包含：
   - 完成时间
   - 完成任务列表
   - 修改文件列表
   - 验证命令
   - 验证结果
   - 未解决风险
   - 建议下一步
6. 在 logs/codex-run.log 末尾追加：

```
ALL TASKS COMPLETED: 当前时间
```

7. 停止继续调用 Codex CLI，等待人工确认。

```
---# 三、其他文件最小模板这些文件不需要太复杂，关键是让 Hermes 有地方写状态。## `TASKS.md````markdown# TASKS## 当前大目标在这里写当前大任务目标。## 任务清单- [ ] Task 1：- [ ] Task 2：- [ ] Task 3：- [ ] Task 4：- [ ] Task 5：
```

## `PROGRESS.md`

```
# PROGRESS## 当前状态- 当前任务：- 当前小点：- 状态：- 最近更新时间：## 最近完成的小点| 时间 | 任务 | 小点 | 结果 | 验证 ||---|---|---|---|---||  |  |  |  |  |## 当前卡点- 无## 下一步- 读取 TASKS.md，继续第一个未完成任务。## 最近验证- 尚未验证。
```

## `TASK_HANDOFF.md`

```
# TASK_HANDOFF## 当前目标待补充。## 当前任务待补充。## 已完成内容- 待补充。## 未完成内容- 待补充。## 已修改文件- 待补充。## 当前卡点- 无。## 最近中断记录| 时间 | 类型 | 原因 | 当前任务 | 下一步 ||---|---|---|---|---||  |  |  |  |  |## Token / Rate Limit 记录| 时间 | 错误类型 | 错误内容 | 重试次数 | 下一次重试 | 是否恢复 ||---|---|---|---|---|---||  |  |  |  |  |  |
```

## `VALIDATION.md`

最小版：

```
# VALIDATION## 验证命令```bashgit diff --check
```

## 通过标准

- 命令返回 0。
- 没有明显语法错误。
- PROGRESS.md 已更新。
- TASK_HANDOFF.md 已更新。

```
---# 四、初始化命令在项目根目录执行一次：```bashmkdir -p logstouch AGENTS.mdtouch TASKS.mdtouch PROGRESS.mdtouch TASK_HANDOFF.mdtouch VALIDATION.mdtouch logs/codex-run.logtouch logs/codex-retry.log
```

然后把上面的内容分别写进去。

---

最终建议是：**长规则写进 `AGENTS.md`，启动指令只保留“重试优先、读取文件、执行第一个未完成任务”这几个关键点。**这样比每次给一大段提示更稳。
